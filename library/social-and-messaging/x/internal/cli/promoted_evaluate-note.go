// Copyright 2026 cathryn-lavery. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newEvaluateNotePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyNoteText string
	var bodyPostId string

	cmd := &cobra.Command{
		Use:         "evaluate-note",
		Short:       "Endpoint to evaluate a community note.",
		Long:        "Shortcut for 'evaluate-note evaluate-community-notes'. Endpoint to evaluate a community note.",
		Example:     "  x-pp-cli evaluate-note --note-text example-value",
		Annotations: map[string]string{"pp:endpoint": "evaluate-note.evaluate-community-notes", "pp:method": "POST", "pp:path": "/2/evaluate_note"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("note-text") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "note-text")
			}
			if !cmd.Flags().Changed("post-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "post-id")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2/evaluate_note"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyNoteText != "" {
				body["note_text"] = bodyNoteText
			}
			if bodyPostId != "" {
				body["post_id"] = bodyPostId
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyNoteText, "note-text", "", "Text for the community note.")
	cmd.Flags().StringVar(&bodyPostId, "post-id", "", "Unique identifier of this Tweet. This is returned as a string in order to avoid complications with languages and...")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
