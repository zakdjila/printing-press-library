// Copyright 2026 cathryn-lavery. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTweetsSearchPostsRecentCmd(flags *rootFlags) *cobra.Command {
	var flagQuery string
	var flagStartTime string
	var flagEndTime string
	var flagSinceId string
	var flagUntilId string
	var flagMaxResults int
	var flagNextToken string
	var flagPaginationToken string
	var flagSortOrder string
	var flagTweetFields string
	var flagExpansions string
	var flagMediaFields string
	var flagPollFields string
	var flagUserFields string
	var flagPlaceFields string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "search-posts-recent",
		Short:       "Retrieves Posts from the last 7 days matching a search query.",
		Example:     "  x-pp-cli tweets search-posts-recent --query example-value",
		Annotations: map[string]string{"pp:endpoint": "tweets.search-posts-recent", "pp:method": "GET", "pp:path": "/2/tweets/search/recent", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("query") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "query")
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"recency", "relevancy"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "sort-order", flagSortOrder, allowedSortOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2/tweets/search/recent"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "tweets", path, map[string]string{
				"query":            fmt.Sprintf("%v", flagQuery),
				"start_time":       fmt.Sprintf("%v", flagStartTime),
				"end_time":         fmt.Sprintf("%v", flagEndTime),
				"since_id":         fmt.Sprintf("%v", flagSinceId),
				"until_id":         fmt.Sprintf("%v", flagUntilId),
				"max_results":      fmt.Sprintf("%v", flagMaxResults),
				"next_token":       fmt.Sprintf("%v", flagNextToken),
				"pagination_token": fmt.Sprintf("%v", flagPaginationToken),
				"sort_order":       fmt.Sprintf("%v", flagSortOrder),
				"tweet.fields":     fmt.Sprintf("%v", flagTweetFields),
				"expansions":       fmt.Sprintf("%v", flagExpansions),
				"media.fields":     fmt.Sprintf("%v", flagMediaFields),
				"poll.fields":      fmt.Sprintf("%v", flagPollFields),
				"user.fields":      fmt.Sprintf("%v", flagUserFields),
				"place.fields":     fmt.Sprintf("%v", flagPlaceFields),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagQuery, "query", "", "One query/rule/filter for matching Posts. Refer to https://t.co/rulelength to identify the max query length.")
	cmd.Flags().StringVar(&flagStartTime, "start-time", "", "YYYY-MM-DDTHH:mm:ssZ. The oldest UTC timestamp from which the Posts will be provided. Timestamp is in second...")
	cmd.Flags().StringVar(&flagEndTime, "end-time", "", "YYYY-MM-DDTHH:mm:ssZ. The newest, most recent UTC timestamp to which the Posts will be provided. Timestamp is in...")
	cmd.Flags().StringVar(&flagSinceId, "since-id", "", "Returns results with a Post ID greater than (that is, more recent than) the specified ID.")
	cmd.Flags().StringVar(&flagUntilId, "until-id", "", "Returns results with a Post ID less than (that is, older than) the specified ID.")
	cmd.Flags().IntVar(&flagMaxResults, "max-results", 10, "The maximum number of search results to be returned by a request.")
	cmd.Flags().StringVar(&flagNextToken, "next-token", "", "This parameter is used to get the next 'page' of results. The value used with the parameter is pulled directly from...")
	cmd.Flags().StringVar(&flagPaginationToken, "pagination-token", "", "This parameter is used to get the next 'page' of results. The value used with the parameter is pulled directly from...")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "This order in which to return results. (one of: recency, relevancy)")
	cmd.Flags().StringVar(&flagTweetFields, "tweet-fields", "", "A comma separated list of Tweet fields to display.")
	cmd.Flags().StringVar(&flagExpansions, "expansions", "", "A comma separated list of fields to expand.")
	cmd.Flags().StringVar(&flagMediaFields, "media-fields", "", "A comma separated list of Media fields to display.")
	cmd.Flags().StringVar(&flagPollFields, "poll-fields", "", "A comma separated list of Poll fields to display.")
	cmd.Flags().StringVar(&flagUserFields, "user-fields", "", "A comma separated list of User fields to display.")
	cmd.Flags().StringVar(&flagPlaceFields, "place-fields", "", "A comma separated list of Place fields to display.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
