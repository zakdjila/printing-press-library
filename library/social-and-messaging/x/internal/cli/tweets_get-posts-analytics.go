// Copyright 2026 cathryn-lavery. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTweetsGetPostsAnalyticsCmd(flags *rootFlags) *cobra.Command {
	var flagIds string
	var flagEndTime string
	var flagStartTime string
	var flagGranularity string
	var flagAnalyticsFields string

	cmd := &cobra.Command{
		Use:         "get-posts-analytics",
		Aliases:     []string{"list"},
		Short:       "Retrieves analytics data for specified Posts within a defined time range.",
		Example:     "  x-pp-cli tweets get-posts-analytics --ids example-value --end-time 2026-01-15T09:00:00Z --start-time 2026-01-15T09:00:00Z --granularity example-value",
		Annotations: map[string]string{"pp:endpoint": "tweets.get-posts-analytics", "pp:method": "GET", "pp:path": "/2/tweets/analytics", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("ids") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "ids")
			}
			if !cmd.Flags().Changed("end-time") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-time")
			}
			if !cmd.Flags().Changed("start-time") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-time")
			}
			if cmd.Flags().Changed("granularity") {
				allowedGranularity := []string{"hourly", "daily", "weekly", "total"}
				validGranularity := false
				for _, v := range allowedGranularity {
					if flagGranularity == v {
						validGranularity = true
						break
					}
				}
				if !validGranularity {
					fmt.Fprintf(os.Stderr, "warning: --%s %q not in allowed set %v\n", "granularity", flagGranularity, allowedGranularity)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2/tweets/analytics"
			params := map[string]string{}
			if flagIds != "" {
				params["ids"] = fmt.Sprintf("%v", flagIds)
			}
			if flagEndTime != "" {
				params["end_time"] = fmt.Sprintf("%v", flagEndTime)
			}
			if flagStartTime != "" {
				params["start_time"] = fmt.Sprintf("%v", flagStartTime)
			}
			if flagGranularity != "" {
				params["granularity"] = fmt.Sprintf("%v", flagGranularity)
			}
			if flagAnalyticsFields != "" {
				params["analytics.fields"] = fmt.Sprintf("%v", flagAnalyticsFields)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "tweets", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagIds, "ids", "", "A comma separated list of Post IDs. Up to 100 are allowed in a single request.")
	cmd.Flags().StringVar(&flagEndTime, "end-time", "", "YYYY-MM-DDTHH:mm:ssZ. The UTC timestamp representing the end of the time range.")
	cmd.Flags().StringVar(&flagStartTime, "start-time", "", "YYYY-MM-DDTHH:mm:ssZ. The UTC timestamp representing the start of the time range.")
	cmd.Flags().StringVar(&flagGranularity, "granularity", "total", "The granularity for the search counts results. (one of: hourly, daily, weekly, total)")
	cmd.Flags().StringVar(&flagAnalyticsFields, "analytics-fields", "", "A comma separated list of Analytics fields to display.")

	return cmd
}
