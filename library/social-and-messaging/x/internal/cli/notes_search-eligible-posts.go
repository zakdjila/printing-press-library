// Copyright 2026 cathryn-lavery. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newNotesSearchEligiblePostsCmd(flags *rootFlags) *cobra.Command {
	var flagTestMode bool
	var flagPaginationToken string
	var flagMaxResults int
	var flagPostSelection string
	var flagTweetFields string
	var flagExpansions string
	var flagMediaFields string
	var flagPollFields string
	var flagUserFields string
	var flagPlaceFields string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "search-eligible-posts",
		Short:       "Returns all the posts that are eligible for community notes.",
		Example:     "  x-pp-cli notes search-eligible-posts --test-mode true",
		Annotations: map[string]string{"pp:endpoint": "notes.search-eligible-posts", "pp:method": "GET", "pp:path": "/2/notes/search/posts_eligible_for_notes", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("test-mode") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "test-mode")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2/notes/search/posts_eligible_for_notes"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "notes", path, map[string]string{
				"test_mode":        fmt.Sprintf("%v", flagTestMode),
				"pagination_token": fmt.Sprintf("%v", flagPaginationToken),
				"max_results":      fmt.Sprintf("%v", flagMaxResults),
				"post_selection":   fmt.Sprintf("%v", flagPostSelection),
				"tweet.fields":     fmt.Sprintf("%v", flagTweetFields),
				"expansions":       fmt.Sprintf("%v", flagExpansions),
				"media.fields":     fmt.Sprintf("%v", flagMediaFields),
				"poll.fields":      fmt.Sprintf("%v", flagPollFields),
				"user.fields":      fmt.Sprintf("%v", flagUserFields),
				"place.fields":     fmt.Sprintf("%v", flagPlaceFields),
			}, nil, flagAll, "", "", "")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagTestMode, "test-mode", false, "If true, return a list of posts that are for the test. If false, return a list of posts that the bots can write...")
	cmd.Flags().StringVar(&flagPaginationToken, "pagination-token", "", "Pagination token to get next set of posts eligible for notes.")
	cmd.Flags().IntVar(&flagMaxResults, "max-results", 10, "Max results to return.")
	cmd.Flags().StringVar(&flagPostSelection, "post-selection", "", "The selection of posts to return. Valid values are 'feed_size: [small|large|xl|xxl], feed_lang: [en|es|...|all]'....")
	cmd.Flags().StringVar(&flagTweetFields, "tweet-fields", "", "A comma separated list of Tweet fields to display.")
	cmd.Flags().StringVar(&flagExpansions, "expansions", "", "A comma separated list of fields to expand.")
	cmd.Flags().StringVar(&flagMediaFields, "media-fields", "", "A comma separated list of Media fields to display.")
	cmd.Flags().StringVar(&flagPollFields, "poll-fields", "", "A comma separated list of Poll fields to display.")
	cmd.Flags().StringVar(&flagUserFields, "user-fields", "", "A comma separated list of User fields to display.")
	cmd.Flags().StringVar(&flagPlaceFields, "place-fields", "", "A comma separated list of Place fields to display.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
