// Copyright 2026 zakdjila. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCallCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssistant string
	var bodyAssistantId string
	var bodyAssistantOverrides string
	var bodyCustomer string
	var bodyCustomerId string
	var bodyCustomers string
	var bodyName string
	var bodyPhoneNumber string
	var bodyPhoneNumberId string
	var bodySchedulePlan string
	var bodySquad string
	var bodySquadId string
	var bodySquadOverrides string
	var bodyTransport string
	var bodyWorkflow string
	var bodyWorkflowId string
	var bodyWorkflowOverrides string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create Call",
		Example: "  vapi-pp-cli call create",
		Annotations: map[string]string{"pp:endpoint": "call.create", "pp:method": "POST", "pp:path": "/call"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/call"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssistant != "" {
					var parsedAssistant any
					if err := json.Unmarshal([]byte(bodyAssistant), &parsedAssistant); err != nil {
						return fmt.Errorf("parsing --assistant JSON: %w", err)
					}
					body["assistant"] = parsedAssistant
				}
				if bodyAssistantId != "" {
					body["assistantId"] = bodyAssistantId
				}
				if bodyAssistantOverrides != "" {
					var parsedAssistantOverrides any
					if err := json.Unmarshal([]byte(bodyAssistantOverrides), &parsedAssistantOverrides); err != nil {
						return fmt.Errorf("parsing --assistant-overrides JSON: %w", err)
					}
					body["assistantOverrides"] = parsedAssistantOverrides
				}
				if bodyCustomer != "" {
					var parsedCustomer any
					if err := json.Unmarshal([]byte(bodyCustomer), &parsedCustomer); err != nil {
						return fmt.Errorf("parsing --customer JSON: %w", err)
					}
					body["customer"] = parsedCustomer
				}
				if bodyCustomerId != "" {
					body["customerId"] = bodyCustomerId
				}
				if bodyCustomers != "" {
					var parsedCustomers any
					if err := json.Unmarshal([]byte(bodyCustomers), &parsedCustomers); err != nil {
						return fmt.Errorf("parsing --customers JSON: %w", err)
					}
					body["customers"] = parsedCustomers
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPhoneNumber != "" {
					var parsedPhoneNumber any
					if err := json.Unmarshal([]byte(bodyPhoneNumber), &parsedPhoneNumber); err != nil {
						return fmt.Errorf("parsing --phone-number JSON: %w", err)
					}
					body["phoneNumber"] = parsedPhoneNumber
				}
				if bodyPhoneNumberId != "" {
					body["phoneNumberId"] = bodyPhoneNumberId
				}
				if bodySchedulePlan != "" {
					var parsedSchedulePlan any
					if err := json.Unmarshal([]byte(bodySchedulePlan), &parsedSchedulePlan); err != nil {
						return fmt.Errorf("parsing --schedule-plan JSON: %w", err)
					}
					body["schedulePlan"] = parsedSchedulePlan
				}
				if bodySquad != "" {
					var parsedSquad any
					if err := json.Unmarshal([]byte(bodySquad), &parsedSquad); err != nil {
						return fmt.Errorf("parsing --squad JSON: %w", err)
					}
					body["squad"] = parsedSquad
				}
				if bodySquadId != "" {
					body["squadId"] = bodySquadId
				}
				if bodySquadOverrides != "" {
					var parsedSquadOverrides any
					if err := json.Unmarshal([]byte(bodySquadOverrides), &parsedSquadOverrides); err != nil {
						return fmt.Errorf("parsing --squad-overrides JSON: %w", err)
					}
					body["squadOverrides"] = parsedSquadOverrides
				}
				if bodyTransport != "" {
					var parsedTransport any
					if err := json.Unmarshal([]byte(bodyTransport), &parsedTransport); err != nil {
						return fmt.Errorf("parsing --transport JSON: %w", err)
					}
					body["transport"] = parsedTransport
				}
				if bodyWorkflow != "" {
					var parsedWorkflow any
					if err := json.Unmarshal([]byte(bodyWorkflow), &parsedWorkflow); err != nil {
						return fmt.Errorf("parsing --workflow JSON: %w", err)
					}
					body["workflow"] = parsedWorkflow
				}
				if bodyWorkflowId != "" {
					body["workflowId"] = bodyWorkflowId
				}
				if bodyWorkflowOverrides != "" {
					var parsedWorkflowOverrides any
					if err := json.Unmarshal([]byte(bodyWorkflowOverrides), &parsedWorkflowOverrides); err != nil {
						return fmt.Errorf("parsing --workflow-overrides JSON: %w", err)
					}
					body["workflowOverrides"] = parsedWorkflowOverrides
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "call",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAssistant, "assistant", "", "This is the assistant that will be used for the call. To use an existing assistant, use `assistantId` instead. To...")
	cmd.Flags().StringVar(&bodyAssistantId, "assistant-id", "", "This is the assistant ID that will be used for the call. To use a transient assistant, use `assistant` instead. To...")
	cmd.Flags().StringVar(&bodyAssistantOverrides, "assistant-overrides", "", "These are the overrides for the `assistant` or `assistantId`'s settings and template variables.")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "This is the customer that will be called. To call an existing customer, use `customerId` instead. Only relevant for...")
	cmd.Flags().StringVar(&bodyCustomerId, "customer-id", "", "This is the customer that will be called. To call a transient customer , use `customer` instead. Only relevant for...")
	cmd.Flags().StringVar(&bodyCustomers, "customers", "", "This is used to issue batch calls to multiple customers. Only relevant for `outboundPhoneCall`. To call a single...")
	cmd.Flags().StringVar(&bodyName, "name", "", "This is the name of the call. This is just for your own reference.")
	cmd.Flags().StringVar(&bodyPhoneNumber, "phone-number", "", "This is the phone number that will be used for the call. To use an existing number, use `phoneNumberId` instead....")
	cmd.Flags().StringVar(&bodyPhoneNumberId, "phone-number-id", "", "This is the phone number that will be used for the call. To use a transient number, use `phoneNumber` instead. Only...")
	cmd.Flags().StringVar(&bodySchedulePlan, "schedule-plan", "", "This is the schedule plan of the call.")
	cmd.Flags().StringVar(&bodySquad, "squad", "", "This is a squad that will be used for the call. To use an existing squad, use `squadId` instead. To start a call...")
	cmd.Flags().StringVar(&bodySquadId, "squad-id", "", "This is the squad that will be used for the call. To use a transient squad, use `squad` instead. To start a call...")
	cmd.Flags().StringVar(&bodySquadOverrides, "squad-overrides", "", "These are the overrides for the `squad` or `squadId`'s member settings and template variables. This will apply to...")
	cmd.Flags().StringVar(&bodyTransport, "transport", "", "This is the transport of the call.")
	cmd.Flags().StringVar(&bodyWorkflow, "workflow", "", "This is a workflow that will be used for the call. To use an existing workflow, use `workflowId` instead. To start a...")
	cmd.Flags().StringVar(&bodyWorkflowId, "workflow-id", "", "This is the workflow that will be used for the call. To use a transient workflow, use `workflow` instead. To start a...")
	cmd.Flags().StringVar(&bodyWorkflowOverrides, "workflow-overrides", "", "These are the overrides for the `workflow` or `workflowId`'s settings and template variables.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
