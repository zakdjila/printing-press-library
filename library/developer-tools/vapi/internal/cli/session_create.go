// Copyright 2026 zakdjila. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSessionCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAssistant string
	var bodyAssistantId string
	var bodyAssistantOverrides string
	var bodyCustomer string
	var bodyCustomerId string
	var bodyExpirationSeconds float64
	var bodyMessages string
	var bodyName string
	var bodyPhoneNumber string
	var bodyPhoneNumberId string
	var bodySquad string
	var bodySquadId string
	var bodyStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create Session",
		Example: "  vapi-pp-cli session create",
		Annotations: map[string]string{"pp:endpoint": "session.create", "pp:method": "POST", "pp:path": "/session"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/session"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssistant != "" {
					var parsedAssistant any
					if err := json.Unmarshal([]byte(bodyAssistant), &parsedAssistant); err != nil {
						return fmt.Errorf("parsing --assistant JSON: %w", err)
					}
					body["assistant"] = parsedAssistant
				}
				if bodyAssistantId != "" {
					body["assistantId"] = bodyAssistantId
				}
				if bodyAssistantOverrides != "" {
					var parsedAssistantOverrides any
					if err := json.Unmarshal([]byte(bodyAssistantOverrides), &parsedAssistantOverrides); err != nil {
						return fmt.Errorf("parsing --assistant-overrides JSON: %w", err)
					}
					body["assistantOverrides"] = parsedAssistantOverrides
				}
				if bodyCustomer != "" {
					var parsedCustomer any
					if err := json.Unmarshal([]byte(bodyCustomer), &parsedCustomer); err != nil {
						return fmt.Errorf("parsing --customer JSON: %w", err)
					}
					body["customer"] = parsedCustomer
				}
				if bodyCustomerId != "" {
					body["customerId"] = bodyCustomerId
				}
				if bodyExpirationSeconds != 0.0 {
					body["expirationSeconds"] = bodyExpirationSeconds
				}
				if bodyMessages != "" {
					var parsedMessages any
					if err := json.Unmarshal([]byte(bodyMessages), &parsedMessages); err != nil {
						return fmt.Errorf("parsing --messages JSON: %w", err)
					}
					body["messages"] = parsedMessages
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPhoneNumber != "" {
					var parsedPhoneNumber any
					if err := json.Unmarshal([]byte(bodyPhoneNumber), &parsedPhoneNumber); err != nil {
						return fmt.Errorf("parsing --phone-number JSON: %w", err)
					}
					body["phoneNumber"] = parsedPhoneNumber
				}
				if bodyPhoneNumberId != "" {
					body["phoneNumberId"] = bodyPhoneNumberId
				}
				if bodySquad != "" {
					var parsedSquad any
					if err := json.Unmarshal([]byte(bodySquad), &parsedSquad); err != nil {
						return fmt.Errorf("parsing --squad JSON: %w", err)
					}
					body["squad"] = parsedSquad
				}
				if bodySquadId != "" {
					body["squadId"] = bodySquadId
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "session",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAssistant, "assistant", "", "This is the assistant configuration for this session. Use this when creating a new assistant configuration. If...")
	cmd.Flags().StringVar(&bodyAssistantId, "assistant-id", "", "This is the ID of the assistant associated with this session. Use this when referencing an existing assistant.")
	cmd.Flags().StringVar(&bodyAssistantOverrides, "assistant-overrides", "", "These are the overrides for the assistant configuration. Use this to provide variable values and other overrides...")
	cmd.Flags().StringVar(&bodyCustomer, "customer", "", "This is the customer information associated with this session.")
	cmd.Flags().StringVar(&bodyCustomerId, "customer-id", "", "This is the customerId of the customer associated with this session.")
	cmd.Flags().Float64Var(&bodyExpirationSeconds, "expiration-seconds", 0.0, "Session expiration time in seconds. Defaults to 24 hours (86400 seconds) if not set.")
	cmd.Flags().StringVar(&bodyMessages, "messages", "", "This is an array of chat messages in the session.")
	cmd.Flags().StringVar(&bodyName, "name", "", "This is a user-defined name for the session. Maximum length is 40 characters.")
	cmd.Flags().StringVar(&bodyPhoneNumber, "phone-number", "", "This is the phone number configuration for this session.")
	cmd.Flags().StringVar(&bodyPhoneNumberId, "phone-number-id", "", "This is the ID of the phone number associated with this session.")
	cmd.Flags().StringVar(&bodySquad, "squad", "", "This is the squad configuration for this session. Use this when creating a new squad configuration. If squadId is...")
	cmd.Flags().StringVar(&bodySquadId, "squad-id", "", "This is the squad ID associated with this session. Use this when referencing an existing squad.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "This is the current status of the session. Can be either 'active' or 'completed'.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
